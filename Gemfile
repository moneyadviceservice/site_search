source 'https://rubygems.org'

git_source(:github) { |repo_name| "https://github.com/#{repo_name}" }

# Specify your gem's dependencies in site_search.gemspec
gemspec

group :development, :test do
  gem 'dotenv'
  gem 'pry'
end

group :test do
  gem 'vcr'
  gem 'webmock'
end
